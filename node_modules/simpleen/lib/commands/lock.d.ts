import { flags } from "@oclif/command";
import Command from "../BaseCommand";
/**
 * Locks the translations of the configured target languages
 * The locked translations will not be changed afterwards
 */
export declare class LockCommand extends Command {
    static description: string;
    static flags: {
        config: flags.IOptionFlag<string>;
        lockFile: flags.IOptionFlag<string>;
    };
    run(): Promise<void>;
}
