import { flags } from "@oclif/command";
import Command from "../BaseCommand";
/**
 * Get current quota / usage of API
 */
export declare class UsageCommand extends Command {
    static description: string;
    static flags: {
        config: flags.IOptionFlag<string>;
    };
    run(): Promise<void>;
}
