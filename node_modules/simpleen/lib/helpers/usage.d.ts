import { SimpleenConfig } from "./config";
export declare type UsageData = {
    plan: {
        name: string;
        maxSegment: number;
    };
    usage: {
        segment: number;
    };
};
/**
 * Translates into language and merge with already translated data
 * @param config loaded simpleen config file
 */
export declare function getUsage(config: SimpleenConfig): Promise<UsageData>;
